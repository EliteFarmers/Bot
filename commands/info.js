const { prefix } = require('../config.json');
module.exports = {
	name: 'info',
	aliases: ['i'],
	description: 'Information',
	usage: '[command name]',
	guildOnly: false,
	execute(message, args) {
			
	},
};
