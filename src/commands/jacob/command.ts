import type { CommandGroupSettings } from "../../classes/Command.js";

const command: CommandGroupSettings = {
	name: 'jacob',
	description: 'Get jacob\'s stats of a player!'
}

export default command;